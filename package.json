{
  "name": "base-proyect-with-agent-guidelines",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:infra": "docker compose -f docker/docker-compose.yml up -d",
    "stop:infra": "docker compose -f docker/docker-compose.yml down",
    "test": "pnpm -r test",
    "agent:finalize": "node scripts/agent-finalize.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
